# MindVerse 测试用例文档（TestCase）

> 版本：v0.1.0-draft | 日期：2026-09-01 | 状态：设计阶段
> 单测 = Vitest 自动化；手测 = 浏览器人工验证

## 1. 人格管理

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| P-01 | 预设人格列表加载 | 手测 | 打开通讯录 | ≥8 个预设人格，含头像/简介/标签 |
| P-02 | 创建人格必填校验 | 手测 | 名字或 System Prompt 留空 | 保存按钮 opacity-30 禁用；填满后可点 |
| P-03 | 创建自定义人格 | 手测 | 填全字段保存 | 列表出现新人格；刷新页面仍在（IndexedDB 持久化） |
| P-04 | 编辑人格 | 手测 | 改名与设定保存 | 会话内该人格新回复使用新设定；历史消息不变 |
| P-05 | 删除人格弹窗确认 | 手测 | 删除自定义人格 | 出现 ConfirmDialog（非系统 confirm）；确认后从列表消失 |
| P-06 | 预设人格保护 | 手测 | 尝试删除/覆盖预设 | 仅允许「另存为副本」，原件不可删改 |
| P-07 | 头像自动生成 | 手测 | 不传头像创建「阿明」 | 主题色圆角块 +「阿」字；图片头像完整显示不裁切 |
| P-08 | 标签上限 | 手测 | 添加第 6 个性格标签 | 无法继续添加，提示上限 5 个 |
| P-09 | 人格生成器（P1） | 手测 | 输入描述生成 | 产出完整档案草稿；取消则不落库 |
| P-10 | 人格分组（2026-09-05） | 手测 | 编辑人格填分组「工作」保存；再编辑另一人不填 | 通讯录中同组人格归入「工作」分组头下（带数量，可点击折叠/展开）；未分组人格直接列在列表顶部无分组头 |
| P-11 | 分组名联想与清理 | 手测 | 已有「工作」组时新建人格，分组框聚焦 | 下拉列出已有分组可点选；输入纯空格保存视为未分组；分组下所有人格都改空后分组头消失 |
| P-12 | 预设人格仅改分组（2026-09-05） | 手测 | 编辑预设人格（如理性分析师），只改分组保存 | 原人格直接进入该分组（无「（副本）」产生）；再改名字/设定保存则另存副本 |

## 2. 私聊

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| C-01 | 发送消息 | 手测 | 输入文本回车 | 用户气泡右对齐；AI 流式回复左对齐带头像人格名 |
| C-02 | 空输入禁用 | 手测 | 输入框为空 | 发送按钮 opacity-30；Enter/点击均不发送 |
| C-03 | 流式中停止 | 手测 | 生成中点停止 | 输出立即截断，消息标记 stopped，无报错 |
| C-04 | 会话持久化 | 手测 | 刷新页面 | 消息按时间序完整恢复 |
| C-05 | 重新生成（P1） | 手测 | 点最后一条 AI 回复的重新生成 | 旧内容被替换；过程可停止 |
| C-06 | Shift+Enter 换行 | 手测 | Shift+Enter | 换行不发送 |
| C-07 | cmd/ctrl+A 全选 | 手测 | 输入框内 cmd+A | 全选输入框文本 |
| C-08 | 复制消息 | 手测 | hover 消息点复制 | Toast「已经复制到粘贴板」，剪贴板内容正确 |
| C-09 | 思考模型回复（2026-09-05） | 手测 | 人格绑定 reasoning 模型（如 deepseek-r1 类）发消息 | 气泡内出现「思考中…」折叠块实时流入推理文本；正文随后流出；完成后思考块默认收起，点开可查看 |
| C-10 | 思考内容持久化 | 手测 | 思考模型回复完成后刷新页面 | 消息恢复，思考过程仍可展开查看；非思考模型消息无思考块 |
| C-11 | 发送图片（2026-09-05） | 手测 | 点文件按钮选图片（或直接 Ctrl+V 粘贴截图）发送 | 输入框上方出现缩略图附件条；发送后气泡内图片完整显示（不裁切）；AI 能描述图片内容（需多模态模型） |
| C-12 | 图片全屏预览 | 手测 | 点击气泡内图片 | 全屏遮罩预览，Esc/点击任意处关闭 |
| C-13 | 发送文本文件 | 手测 | 上传 .md/.txt/.csv 文件并发送 | 气泡显示文件卡片（名+大小）；AI 回复中体现对文件内容的理解 |
| C-14 | 其他类型文件 | 手测 | 上传 .zip/.pdf 发送 | 气泡显示文件卡片；AI 仅见文件名（回复不涉及内容） |
| C-15 | 附件限制 | 手测 | 上传 >10MB 图片 / >200KB 文本 / 第 7 个附件 | 对应 toast 错误提示，不发送 |
| C-16 | 纯附件消息 | 手测 | 不输入文字只发一张图 | 可直接发送（按钮可点），AI 正常回复 |
| C-17 | 空气泡防御（2026-09-05） | 手测 | 构造空正文消息（如空回复完成态/空用户消息） | 气泡整条不渲染，页面无空壳框 |
| C-18 | 流式等待态 | 手测 | 发消息后观察 AI 气泡（首 token 前） | 显示三点打字动画，而非空气泡+光标；首 token 到达后正常流式输出 |
| C-19 | 会话列表收起/展开（2026-09-06） | 手测 | 点聊天窗口头部左侧「收起会话列表」按钮 | 列表宽度动画归零（无右侧残留边框），聊天区占满；按钮变为「展开会话列表」图标，再点恢复 280px |

## 3. 群聊

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| G-01 | 建群校验 | 手测 | 选 1 个人格建群 | 不可建，提示至少 2 个成员 |
| G-02 | @单点名 | 手测 | `@毒舌评论家` 你怎么看 | 仅毒舌评论家回复一次 |
| G-03 | @多人按序 | 手测 | `@A` 和 `@B` 各自观点 | A、B 依次串行回复，顺序与 @ 出现顺序一致 |
| G-04 | 无 @ 走主机智体 | 手测 | 不带 @ 发消息 | 由 host 人格回复；提示条逻辑符合 DesignSystem 6.3 |
| G-05 | 接龙触发 | 手测 | 点「让大家聊聊」设 2 轮、3 成员 | 6 人次发言串行完成，每条有独立人格名与主题色 |
| G-06 | 接龙停止 | 手测 | 接龙中点「停止接龙」 | 当前生成截断，后续排队取消，输入区恢复可用 |
| G-07 | 接龙中禁输入 | 手测 | 接龙中尝试打字发送 | 输入框禁用，placeholder「接龙进行中…」 |
| G-08 | 接龙失败暂停 | 手测 | 模拟 API 失败（改错 Key） | 失败消息红框 + Toast；接龙暂停不自动跳过 |
| G-09 | 成员增删 | 手测 | 群设置里加减人 | 后续发言上下文包含新成员、不含被移除者 |
| G-10 | 人格一致性 | 手测 | 毒舌评论家群内发言 | 口吻符合设定，无「作为 AI」类出戏表述 |
| G-11 | 上下文注入不可见 | 手测 | 检查消息渲染 | System 注入内容不出现在任何气泡里 |
| G-12 | 群名行内编辑（2026-09-05） | 手测 | 群聊标题 hover 出现编辑按钮，点击改名后 Enter；再试 Esc / 清空提交；私聊与 AI 助手标题 hover | 标题更新且刷新后保持（持久化）；Esc 取消不变；空名称不提交；非群聊无编辑按钮 |

## 3.5 收藏（2026-09-01 新增）

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| F-01 | 单条收藏 | 手测 | hover 消息点星标 | Toast「已收藏」；收藏页出现该消息（含发送者） |
| F-02 | 多选收藏 | 手测 | hover 点「多选」→ 勾选 3 条 → 点「收藏(3)」 | 一条收藏含 3 条消息按时间排序；退出多选恢复输入框 |
| F-03 | 不可收藏过滤 | 手测 | 多选模式下点流式中/出错的消息 | 置灰不可勾选；底部按钮计数不含它们 |
| F-04 | 来源跳转 | 手测 | 收藏页点来源会话 | 跳回聊天并选中该会话；会话已删则 Toast「来源会话已被删除」 |
| F-05 | 快照独立 | 手测 | 收藏后删除原会话/原消息 | 收藏内容完整可看（含来源标题快照） |
| F-06 | 删除收藏 | 手测 | hover 收藏卡片点删除 → 确认 | 卡片消失；原消息不受影响 |
| F-07 | 多选退出 | 手测 | 多选中切换左侧会话 | 自动退出多选模式，回到普通聊天 |
| F-08 | 服务器同步 | 手测 | 登录态收藏 → 刷新页面 | 收藏仍在（本地 + 服务器双写） |

## 4. Provider 与设置

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| S-01 | 添加火山方舟 Provider | 手测 | 填 OpenAI 兼容 Base URL + Key + 模型 | 保存成功；连通性测试返回延迟 |
| S-02 | 添加 Anthropic | 手测 | 填 Anthropic 兼容配置 | 流式回复正常（messages 协议 adapter） |
| S-03 | 自定义 OpenAI 兼容 | 手测 | 任意兼容服务 | 正常对话 |
| S-04 | 错误 Key | 手测 | 填错 Key 发消息 | Toast 明确提示 provider 名 + 错误，消息标 error，可重试；无静默失败 |
| S-05 | Key 掩码 | 手测 | 设置页查看 Key | 默认掩码；控制台无 Key 明文（只记长度） |
| S-06 | 人格级模型覆盖 | 手测 | A 人格指定模型 X，B 跟随全局 | A 用 X，B 用全局模型（network 面板验证不同端点/模型） |
| S-07 | 多 Provider 切换 | 手测 | 全局默认切换 | 后续未覆盖人格全部用新默认 |

## 5. 超时与容错

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| T-01 | 5 分钟超时 | 手测 | mock 挂起接口 | 5min 触发 abort，Toast 提示超时 |
| T-02 | 网络断开 | 手测 | 生成中断网 | 错误 Toast + 消息 error 状态 + 重试按钮 |
| T-03 | 重试确认 | 手测 | 点失败消息重试 | 弹窗确认后再发 |

## 6. 数据管理

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| D-01 | 删除会话 | 手测 | 删除群/私聊 | ConfirmDialog 确认后列表消失，消息一并清除 |
| D-02 | 导出备份 | 手测 | 设置→通用→导出备份 | 下载 `mindverse-backup-日期-时间.json`，含人格/会话/消息/服务商（含 Key）/设置 |
| D-03 | 导入备份 | 手测 | 选非备份文件导入；再导入 D-02 文件 | 非法文件 Toast 报错；合法文件先弹覆盖确认，确认后数据全量恢复并自动刷新页面 |
| D-04 | 刷新数据完整 | 手测 | 建人格+两轮对话后刷新 | 全部数据在 |
| D-05 | 端口固定 | 手测 | 5175 被占时再起 dev | 启动报错提示端口被占，不换端口 |

## 6.5 皮肤主题系统（2026-09-01 新增）

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| T-01 | 切换皮肤 | 手测 | 设置→主题皮肤→逐个点击 6 套皮肤 | 立即生效，约 300ms 平滑渐变；当前皮肤卡片有描边 + 对勾 |
| T-02 | 持久化 | 手测 | 切到非默认皮肤→刷新页面 | 皮肤保持，刷新无默认色闪白（首帧前应用） |
| T-03 | 深色皮肤可读性 | 手测 | 墨夜/蓝色幻想/哥特虚空下过一遍：会话列表、气泡、输入区、设置、人格库、收藏、Toast、确认弹窗 | 文字/边框/hover 全部可读，无残留白底黑字 |
| T-04 | 气泡与头像 | 手测 | 每套皮肤下发一条消息（私聊） | 我方/对方气泡颜色与文字色匹配皮肤；「我」头像、Logo 渐变跟随 |
| T-05 | 危险操作 | 手测 | 任意皮肤下走删除确认 / 停止生成 hover | 危险红 #E5484D 统一，hover 底为透明红不刺眼 |
| T-06 | 细节 | 手测 | 任意皮肤下：选中文本、滚动列表、focus 搜索框/输入框 | 选区与滚动条跟随主题；focus 描边为主题蓝 |
| T-07 | 背景壁纸 | 手测 | 切到哥特虚空 | 壁纸铺满整窗（滚动消息时壁纸不动），UI 半透明浮在壁纸上，文字可读 |
| T-08 | 壁纸切换 | 手测 | 哥特虚空 ↔ 其他皮肤来回切 | 壁纸出现/消失干净，无残影；无壁纸皮肤恢复纯色 |
| T-09 | 壁纸可读性 | 手测 | 哥特虚空下过：气泡、设置弹窗、Toast、确认框、输入区 | 蒙层足够，所有文字清晰可读（不清就调 --mv-bg-mask 透明度） |

## 7. 单元测试（Vitest）

| # | 模块 | 断言要点 |
| --- | --- | --- |
| U-01 | SSE 解析器 | OpenAI/Anthropic 两种 chunk 格式归一；`[DONE]`、注释行、半包粘包容错 |
| U-02 | @提及解析 | `@A 你好 @B` → [A,B]；不完整 `@`、重名前缀、不存在的名字处理 |
| U-03 | 调度器状态机 | 串行执行顺序、stop 中断后不再调度、失败暂停不跳过 |
| U-04 | PromptBuilder | 群聊注入成员摘要与轮次信息；私聊不含群信息；API Key 永不出现在 prompt |
| U-05 | 备份序列化 | 导出结构字段完整（app/version/data）；非法文件（缺 app 标识）导入抛错 |

## 8. 服务端与认证（二期）

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| A-01 | 注册 | curl | POST /api/auth/register | 返回 token；密码以 argon2 哈希落库，无明文 |
| A-02 | 登录 | curl | 正确/错误密码各一次 | 正确返回 token；错误返回 401 统一错误结构 |
| A-03 | 重复用户名 | curl | 注册同名用户两次 | 第二次返回 409 |
| A-04 | 未带 token 访问 | curl | 不带 Authorization 调 /api/personas | 401 |
| A-05 | 无效 token | curl | 伪造/过期 token | 401 |
| I-01 | 数据隔离 | curl | 用户 A、B 各建人格，A 拉列表 | A 只见自己的 + 公共预设，不见 B 的 |
| I-02 | 越权访问 | curl | A 用 B 的资源 id 调 PUT/DELETE | 404（不暴露存在性） |
| I-03 | Provider Key 不回传 | 手测 | GET /api/providers 看响应 | 响应体无 apiKey 明文字段 |
| I-04 | LLM 代理流式 | 手测 | POST /api/chat（SSE） | delta 事件逐段到达，结束有 done 事件；无 CORS 报错 |
| I-05 | LLM 代理错误 | 手测 | 服务商配错 Key 走代理 | SSE error 事件 + 前端 Toast，无静默失败 |
| I-06 | 上游超时 | 手测 | mock 挂起上游 | 5 分钟超时断流并返回错误（chunk 重置计时） |
| M-01 | 多端同步 | 手测 | 浏览器 A 发消息，浏览器 B 同账号刷新 | B 能拉到 A 的消息（增量接口） |
| M-02 | 双模式切换 | 手测 | 设置里切本地↔服务器模式 | 未登录只能本地；切换后各模式数据互不污染 |
| M-03 | 游客模式不受影响 | 手测 | 不登录直接用 | 与一期行为完全一致（本地模式） |

## 8.5 桌面端（Tauri，2026-09-05 新增）

| # | 用例 | 类型 | 步骤 | 预期 |
| --- | --- | --- | --- | --- |
| T-01 | 应用启动 | 手测 | `pnpm tauri dev`（或打开打包的 .app） | 窗口打开无报错；控制台日志出现 `desktop server ready: 127.0.0.1:<port>` |
| T-02 | 内嵌服务随机端口 | 手测 | 连续两次启动应用 | 每次端口不同（127.0.0.1 随机）；无端口冲突报错 |
| T-03 | 服务器模式可用 | 手测 | 桌面端注册/登录后正常聊天 | 走内嵌 server（Network 面板请求指向 127.0.0.1:<port>/api/...） |
| T-04 | 游客模式方舟透传 | 手测 | 不登录，配置方舟 provider（baseURL 自动为 /ark/...）发消息 | 请求经内嵌 server /ark 转发，流式回复正常，无 CORS 报错 |
| T-05 | 数据目录 | 手测 | 桌面端产生数据后查看 `~/Library/Application Support/com.mindverse.desktop/` | 存在 mindverse.db（+wal）；首次启动开发库数据已自动迁移 |
| T-06 | 重启登录态保持 | 手测 | 登录后完全退出应用重开 | 仍为登录态（JWT 密钥落盘复用），无需重登 |
| T-07 | 设置面板适配 | 手测 | 桌面端打开设置「服务器」区块 | 不显示「服务器地址」输入框（浏览器版仍显示） |
| T-08 | 浏览器版回归 | 手测 | `pnpm dev` 浏览器打开 localhost:5175 | 行为与桌面端改造前完全一致（vite 代理 /api、/ark 照常） |

## 9. 服务端单元测试（cargo test）

| # | 模块 | 断言要点 |
| --- | --- | --- |
| R-01 | JWT 签发校验 | 过期/篡改 token 拒绝；合法 token 解出 user_id |
| R-02 | 密码哈希 | argon2 verify 正确性；相同密码盐不同哈希不同 |
| R-03 | sqlx 查询 | 各资源 CRUD 均强制 user_id 过滤（越权返回空/404） |
| R-04 | LLM adapter | OpenAI/Anthropic 两种上游 SSE 响应解析归一；5 分钟 chunk 重置超时逻辑 |
| R-05 | 错误映射 | 内部错误统一 `{error:{code,message}}`，不泄漏堆栈 |

## 10. 回归清单（发版前）

- [ ] 全部 P0 用例通过
- [ ] 三种 Provider 真实 Key 各跑一轮私聊+群聊
- [ ] 控制台过滤 `MindVerse:` 无 Key 明文、无 error 残留
- [ ] 首屏空状态 → 添加 Provider → 开聊 全流程 ≤ 5 步
- [ ] （二期）`cargo test` + `cargo clippy` 通过；单二进制在干净服务器跑通 A-01→M-01 全链路
